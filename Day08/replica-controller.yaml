# 1
# apps/v1

apiVersion: v1
kind: ReplicationController
metadata:
  name: nginx-rc
  labels:
    env: demo
spec:
  template:
    metadata:
      name: nginx-demo
      labels:
        env: demo
    spec:
      containers:
      - image: nginx
        name: nginx-container
  replicas: 3

